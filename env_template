# Environment template

; Copy this file to .env or .env.local and fill in values

; Emotion analysis mode: 'online' | 'offline' | 'auto'
; - online: always call the API
; - offline: always use local heuristics
; - auto: online if API key present, otherwise offline
VITE_EMOTION_MODE=auto

; OpenAI (only used in online/auto when key present)
VITE_OPENAI_API_KEY=
VITE_OPENAI_BASE_URL=
; Example base URL: <https://api.openai.com/v1>
VITE_OPENAI_MODEL=gpt-4o-mini

; Notes
; - Only VITE_* env vars are read in the app. Do not use process.env directly.
; - Audio and Texture toggles are configured in src/config/config.ts (not via env):
;   - AUDIO: enable ambient loop and hover SFX, volumes and file paths
;   - TEXTURES: enable PBR for one planet, choose pack and displacement
